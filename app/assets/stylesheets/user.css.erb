.form__wrapper{
  min-height: calc(100vh - 161px);
  margin: 1vh;
  padding: 1vh;
  background-image: url("<%= images_path('sea.png') %>")
  background-repeat: no-repeat;
  background-position: 100% 100%;
}
